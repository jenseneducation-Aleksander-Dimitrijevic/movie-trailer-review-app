import styled from 'styled-components';
import { MoviesContainer, HeaderMovies } from '../Movies/MoviesStyled';

export const DocumentaryContainer = styled(MoviesContainer)`
padding-top: 15em;
`;

export const HeaderDocumentary = styled(HeaderMovies)``;
